export const LOGIN_DOM_ID = "bh-advertiser-login"

export const LOGIN_REQUEST = 'LOGIN_REQUEST'
export const LOGIN_RESPONSE = 'LOGIN_RESPONSE'
export const LOGIN_EMAIL_INPUT = "LOGIN_EMAIL_INPUT"
export const LOGIN_PASSWORD_INPUT = "LOGIN_PASSWORD_INPUT"
export const LOGIN_FORM_INVALID = "LOGIN_FORM_INVALID"
export const LOGIN_FORM_VALID = "LOGIN_FORM_VALID"

export const LOGIN_FORM_CONSTRAINTS = {
    email: {
        presence: true,
        email: true,
    },
    password: {
        presence: true,
        length: {
            minimum: 6,
            message: "must be at least 6 characters"
        }
    }
}