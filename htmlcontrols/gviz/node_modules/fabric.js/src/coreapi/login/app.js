import "material-design-lite/dist/material"
import m from "mithril"

import { store } from './store'
import { LoginContainer } from "./container"
import {
    LOGIN_DOM_ID,
} from "./constants"

export function mount(element) {

    m.mount(element, LoginContainer)
}