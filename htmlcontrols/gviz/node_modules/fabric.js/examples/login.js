import {mount} from "~/coreapi/login/app"

import coreAPI from "~/coreapi/http"

coreAPI.init({
    url: window._BH_LOGIN_CONFIG.coreApiUrl,
    headers: {
        "Content-Type": "application/json",
        "X-Session-Id": window._BH_LOGIN_CONFIG.sessionId,
    }
})

mount(document.body)